<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Template for `AppSecrets.plist` — copy this file to `AppSecrets.plist`
  (in the same folder) and fill in real values. The real file is
  gitignored so credentials never land in source control.

  After creating `AppSecrets.plist`, drag it into Xcode and make sure
  it's included in the Pulsoria target's "Copy Bundle Resources" phase.
-->
<plist version="1.0">
<dict>
    <key>GeniusAPIToken</key>
    <string>YOUR_ROTATED_GENIUS_TOKEN_HERE</string>
</dict>
</plist>
